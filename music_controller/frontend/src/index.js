import React from "react";
import ReactDOM from "react-dom/client"; // <-- Değiştirildi
import App from "./components/App";

const root = ReactDOM.createRoot(document.getElementById("root")); // <-- Değiştirildi
root.render(<App />);